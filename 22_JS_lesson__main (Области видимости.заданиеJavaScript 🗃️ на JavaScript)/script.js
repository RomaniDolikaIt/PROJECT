//todo 1. Напиши функцию, которая создает локальную переменную и выводит её значение.
// Попробуй получить доступ к этой переменной вне функции и объясни, что произошло;

function createSecret() {
    // Создаем локальную переменную внутри функции
    const secretMessage = "Я спрятан внутри функции!";
    console.log("Внутри функции:", secretMessage);
}

// 1. Запускаем функцию
createSecret(); // Выведет: "Внутри функции: Я спрятан внутри функции!"

// 2. Пробуем достучаться до переменной снаружи
console.log("Снаружи функции:", secretMessage); // Ошибка!

/*
Что произошло (объяснение своими словами):
Как только мы попытались выполнить последнюю строчку, JavaScript выдал ошибку:
 ReferenceError: secretMessage is not defined (Переменная не определена).
Жизненный цикл переменной: Переменная secretMessage родилась внутри функции createSecret и
имеет функциональную область видимости (свой допуск). Она существует только до тех пор,
пока функция работает. Как только функция выполнила свой код и закрылась (на фигурной скобке }),
JavaScript автоматически уничтожил эту переменную из памяти компьютера, чтобы она не занимала место.
Для внешнего мира этой переменной просто не существует, поэтому JavaScript честно сознался, что понятия не имеет,
что это за secretMessage, и остановил программу.
 */

//todo 2. Создай блок с условием и объяви переменную внутри него.
// Попробуй получить доступ к этой переменной вне блока и объясни результат;

// Создаем блок с условием, которое всегда выполняется (true)
if (true) {
    // Объявляем переменную ВНУТРИ блока
    const blockTreasure = "Золото внутри блока!";
    console.log("Внутри блока:", blockTreasure); // Выведет: "Внутри блока: Золото внутри блока!"
}

// Пробуем получить доступ к переменной ВНЕ блока
console.log("Вне блока:", blockTreasure); // Ошибка!

/*
Что произошло (объяснение своими словами):
Программа с треском сломалась на последней строчке и выдала нам уже знакомую ошибку:
ReferenceError: blockTreasure is not defined.

Фигурные скобки { } в условии if — это стены блочной области видимости. Переменные,
созданные через const или let, обладают
встроенным чувством приватности. Они согласны жить и работать только внутри тех скобок, где их написали.
Момент уничтожения: Как только движок JavaScript дошел до закрывающей фигурной скобки }, этот блок
официально завершил свою работу. В ту же микросекунду JS "схлопнул" эту область видимости и стёр
переменную blockTreasure из памяти.
Взгляд снаружи: Последний console.log находится снаружи блока. Он пытается найти переменную
с именем blockTreasure на своём уровне (в глобальной области), но там её нет. Заглянуть назад
в закрывшийся блок if он не может.
Итог: Для внешнего кода переменная исчезла вместе с закрытием фигурных скобок, поэтому JS выдал ошибку.
 */

//todo 3. Изучи, что такое hoisting в JavaScript и расскажи своими словами,
// что это такое и какие проблемы с ним связаны.
// Приведи примеры :)

/*
Hoisting (Поднятие) — это, пожалуй, самое странное «поведение» в JavaScript.
Если объяснять совсем на пальцах, представь, что JavaScript — это очень услужливый, но гиперактивный
дворецкий. Перед тем как запустить твой код, он тайно пробегается по всему файлу, находит все объявления
переменных и функций и мысленно переносит (поднимает) их в самый верх файла, на самый первый этаж области
 видимости.
Сам твой файл на диске, конечно, не меняется. Но JavaScript выполняет его именно так, будто всё объявлено
 в самом начале.

В чём разница: Как поднимаются разные переменные?
Вот тут кроется главный подвох, потому что var, let/const и обычные функции поднимаются по-разному.

1. Как поднимается var (Призрак undefined)
Когда дворецкий поднимает var, он не просто переносит имя переменной наверх, он ещё и кладёт в неё
дефолтное значение — undefined.
Что пишем мы:
JavaScript

console.log(ghost); // Выведет: undefined (а не ошибку!)
var ghost = "Каспер";
console.log(ghost); // Выведет: "Каспер"
Как это видит JavaScript из-за хойстинга:
JavaScript

var ghost; // Переменная поднялась наверх и автоматически стала undefined!

console.log(ghost); // JS её видит, поэтому ошибки нет, выводит undefined
ghost = "Каспер";   // И только тут присваивается значение
console.log(ghost); // Выводит "Каспер"

2. Как поднимаются let и const (Временная мертвая зона)
Они ТОЖЕ поднимаются. Но дворецкий поступает с ними жестоко: он переносит их наверх, но запрещает
 к ним прикасаться до тех пор, пока код физически не дойдет до строчки, где они были созданы.
 Это время ожидания называется Временной мертвой зоной (Temporal Dead Zone или TDZ).
Что пишем мы:
JavaScript

console.log(treasure); //  Ошибка: Cannot access 'treasure' before initialization
const treasure = "Золото";
Итог: С let и const JavaScript защищает нас. Он знает, что переменная будет дальше, но ругается ошибкой,
не позволяя нам использовать пустую переменную.

3. Как поднимаются функции (Короли хойстинга)
Обычные функции (Function Declaration) поднимаются наверх полностью — вместе со всем своим телом
(кодом внутри). Это значит, что ты можешь вызвать функцию на первой строчке файла, а написать её на
самой последней!
JavaScript

sayHi(); // Выведет: "Привет!" (Работает идеально!)

function sayHi() {
    console.log("Привет!");
}
Какие проблемы связаны с Hoisting?
Из-за хойстинга (а именно из-за поведения var) в старом коде рождались жуткие, трудноуловимые баги.
Проблема 1: Случайное перезаписывание данных
Представь, что у тебя огромный код на 500 строк. На 10-й строчке ты создал переменную var id = 1. А
на 400-й строчке забыл об этом и в цикле снова написал var id = 5. Из-за хойстинга они сольются в одну
переменную вверху файла, и твой id на 10-й строчке незаметно испортится. С let JavaScript сразу бы
закричал: «Эй, такая переменная уже есть!».
Проблема 2: Иллюзия того, что код работает правильно
Когда код выводит undefined вместо ошибки, это худшее, что может случиться. Программа не падает, она
продолжает работать, но считает математику или выводит на экран пользователю пустоту.
Посмотри на этот классический баг:
JavaScript

var name = "Алиса";

function greet() {
    // Мы думаем, что берем внешнюю Алису, но...
    console.log("Привет, " + name); // Выведет: "Привет, undefined"

    // ...потому что var ниже поднялся наверх этой функции и перекрыл внешнюю Алису!
    var name = "Боб";
}
greet();
Как жить без проблем с хойстингом?
Современное правило в мире JavaScript очень простое:
1. Забудь про var. Используй только let и const. Они блокируют хойстинг своей "мертвой зоной", и
твой код становится предсказуемым.
2. Объявляй переменные в самом начале. Всегда создавай переменные вверху той зоны, где они нужны,
до того, как начнёшь их использовать.
 */

//todo 4*. Творческое задание - попробуй изобразить свое понимание областей видимости,
// нарисовать иллюстрацию к этой теме.

/*
Чтобы визуализировать области видимости, я нарисовал для  карту «Замка Областей Видимости». Она идеально показывает, кто кого видит, а кто заблокирован стенами.
Представь этот замок как архитектурный план  кода:

========================================================================
 ГЛОБАЛЬНАЯ ОБЛАСТЬ ВИДИМОСТИ (Главный двор замка) 🏰
========================================================================
 │  const globalTreasure = "👑 Корона";
 │
 │  (Сюда может прийти КТО УГОДНО и взять Корону. Она лежит прямо во дворе)
 │
 ├───► [ СТЕНА ФУНКЦИИ: function tower() ] 🗼
 │     │
 │     │  const towerSecret = "📜 Свиток заклинаний";
 │     │
 │     │  👀 Из окна этой башни отлично ВИДНО Корону во дворе!
 │     │
 │     ├──────► [ СТЕНА БЛОКА: if (true) ] 📦 (Сундук внутри башни)
 │     │         │
 │     │         │  const chestGold = "💰 Мешок золота";
 │     │         │  var rogueGhost  = "👻 Призрак-разбойник";
 │     │         │
 │     │         │  👀 Сидя внутри сундука, мы видим:
 │     │         │     - Мешок золота (он тут же)
 │     │         │     - Призрака (он тут же)
 │     │         │     - Свиток заклинаний (он этажом выше в башне)
 │     │         │     - Корону (она на улице)
 │     │         │
 │     │         └──────────────────────────────────────────────────────
 │     │
 │     │  ❌ СТЕНА БЛОКА ЗАКРЫЛАСЬ!
 │     │  Мы стоим в коридоре башни.
 │     │  Пытаемся найти "Мешок золота"? -> ОШИБКА! Сундук заперт (const/let).
 │     │  Пытаемся найти "Призрака"?    -> НАШЛИ! (var просочился сквозь стены).
 │     │
 │     └────────────────────────────────────────────────────────────────
 │
 ❌ СТЕНА ФУНКЦИИ ЗАКРЫЛАСЬ!
 Мы стоим во дворе замка.
 Пытаемся вызвать "Свиток заклинаний"? -> ОШИБКА! Во дворе его нет, а в башню не пускают.
 Пытаемся вызвать "Призрака"?          -> ОШИБКА! Даже var не может сбежать из башни функции.
========================================================================

🎨 Как читать этот рисунок ( ментальная карта):
1. Взгляд изнутри наружу (Свет окон): Если ты находишься в самом глубоком подземелье — в Сундуке if —,
для тебя стены прозрачны. Твой код может протянуть руку наверх и забрать и Свиток из башни, и Корону
со двора. Это называется Цепочкой областей видимости (Scope Chain).
2. Взгляд снаружи внутрь (Тонированные стекла): Если ты стоишь на улице (в Глобальной области),
замок кажется тебе монолитной скалой. Ты понятия не имеешь, что лежит внутри башни или внутри сундука.
Для тебя этих переменных не существует (ReferenceError).
3. Суперсила var: Обрати внимание, как var rogueGhost легко прошел сквозь деревянные стенки сундука
if наружу в башню. Но когда он долетел до каменных стен самой башни function, то застрял.
 Функция — единственный надежный рубеж обороны от var.
 */